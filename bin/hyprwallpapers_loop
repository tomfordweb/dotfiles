#!/usr/bin/env bash

set -e

while true; do
  # pro (presentation) mode pins one static image — skip the shuffle so it
  # never gets clobbered mid-meeting. See bin/rice-mode.
  [ "$(rice-mode get)" = "pro" ] || hyprwallpapers
  sleep "$1"
done
