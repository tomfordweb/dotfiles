---
  - name: "Development environment"
    hosts: localhost
    connection: local 
    tasks:
      - name: Create "~/code"
        file:
          path: "{{ ansible_env.HOME }}/code"
          state: directory
    roles:
        - role-shell
        - role-nvim
        - role-tmux
