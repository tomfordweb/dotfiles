---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    roles:
        # Must run first as this sets up bash/zsh environment
        # Many of the roles after this depend on ~/.shell_functions
        - role-i3
